import 'package:flutter/material.dart';
import 'package:material_symbols_icons/symbols.dart';

class NutrimentColors {
  static Color calories = Color(0xFFFF8C00);
  static Color carbs = Color(0xFF32CD32);
  static Color fats = Color(0xFFF5D000);
  static Color proteins = Color(0xFF1E90FF);
}

class NutrimentIcons {
  static IconData calories = Symbols.mode_heat_rounded;
  static IconData carbs = Symbols.nutrition_rounded;
  static IconData fats = Symbols.water_drop_rounded;
  static IconData proteins = Symbols.exercise_rounded;
}
